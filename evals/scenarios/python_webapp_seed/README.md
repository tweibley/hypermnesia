# Synthetic Python Web App Seed

This seed is intentionally small but realistic enough for coding-agent eval tasks:

- FastAPI app with in-memory repositories
- role-based edit rules for tasks
- endpoint tests as deterministic graders

Task overlays copy additional tests and prompt artifacts into the workspace before each trial.
